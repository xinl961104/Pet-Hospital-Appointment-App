<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.comp90018.drpet">
    <uses-feature
        android:name="android.hardware.Camera"
        android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpdateAPetActivity"></activity>
        <activity android:name=".ViewAPetActivity" />
        <activity android:name=".AddPetActivity" />
        <activity android:name=".ViewAllPetsActivity" />
        <activity android:name=".TimeSlotActivity" />
        <activity android:name=".ChooseTimePage2" />
        <activity android:name=".AppointmentAddPetActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".ManagementActivity"
            android:label="@string/title_activity_management"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HospitalActivity"
            android:label="@string/title_activity_hospital" />
        <activity android:name=".DoctorActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ChooseHospitalActivity" />
        <activity android:name=".ShowBookInfoActivity" />
        <activity android:name=".ChooseTimeSlotActivity" />
        <activity android:name=".ChooseDoctorActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SigninActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>